describe('textStyles', () => {
  it('should generate a correct css mixin to be used with a styled component for a given text style', () => {
    // TODO: Work out how exactly we can synchronously test tagged templates.
  });
});
