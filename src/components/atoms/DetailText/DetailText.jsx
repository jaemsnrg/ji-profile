import React from 'react';
import { Wrapper } from './detailText.style';

const DetailText = ({children}) => {
    return (
    <Wrapper>
        {children}
    </Wrapper>)
}

export default DetailText;