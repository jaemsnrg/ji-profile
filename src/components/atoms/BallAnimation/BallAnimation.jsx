import React from 'react'
import {Wrapper} from './ballAnimation.style'

const BallAnimation = (props) => {
    const {width} = props

    return (
        <Wrapper width={width}>
        </Wrapper>
    )
}


export default BallAnimation;
